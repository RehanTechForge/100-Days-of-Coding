const author: string = 'Quaid-e-Azam';
const message: string = "Before you make a decision… “Think a hundred times before you take a decision, but once that decision is taken, stand by it as one man.”";

console.log(`${author} Once Said ${message}`);

